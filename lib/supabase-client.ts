// Supabase client configuration
// TODO: Uncomment and configure when Supabase is integrated

/*
import { createClient } from '@supabase/supabase-js'

const supabaseUrl = process.env.NEXT_PUBLIC_SUPABASE_URL!
const supabaseAnonKey = process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!

export const supabase = createClient(supabaseUrl, supabaseAnonKey)

// Database types can be generated using Supabase CLI
// Run: npx supabase gen types typescript --project-id YOUR_PROJECT_ID > types/database.ts
*/

// Placeholder for future Supabase integration
export const placeholder = true;

